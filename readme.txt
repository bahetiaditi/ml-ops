1. Create an env in this digit classification folder.
   conda create -n "digit"
   conda activate digit
2. After creating , for your code file , write the requirements.txt in your env.
3. pip install -r requirements.txt
4. If successful,run python file.